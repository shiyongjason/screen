import vuex from 'vuex'
import Vue from 'vue'

import linemodule from './module/linemodule'
import piemodule from './module/piemodule'

Vue.use(vuex)
export default new vuex.Store({
  modules: {
    linemodule: linemodule, // 折线图 状态单独维护
    piemodule: piemodule // 饼图 状态单独维护
  }
})
